# 更新记录

## v1.4.3 — 2026-09-08

本次更新重写图片卡片，并修复任务存储、权限判断和异步提醒中的边界问题。
现有指令、LLM 工具、群隔离方式及倒计时 / 正计时的主要使用流程保持不变。

### 全新图片卡片

- 改为奶白底色与深墨绿文字的单页布局，以大号数字突出剩余时间。
- 今日任务使用暖橙色，正计时使用青绿色；列表序号与文字列表保持一致。
- 支持长名称、多行模板和较长剩余时间的自适应排版。
- 增加空列表布局；在像素预算允许时使用抗锯齿渲染，并缓存字体。
- 卡片文件名使用完整会话 ID 的哈希，避免不同会话因字符替换或截断共用文件。
- 保留图片尺寸上限，过大的卡片回退为文字。

### 安全与数据保护

- 限制模板格式宽度、精度和展开长度，防止短模板触发异常内存分配。
- 禁止模板访问对象属性或下标；普通占位符、补零格式和双大括号转义继续可用。
- 数据文件损坏、结构异常、任务 ID 重复或格式版本不受支持时，明确报错并停止写入，保留原文件。
- 保存使用独立临时文件、刷盘和原子替换；保存失败时回滚内存中的修改。
- 加载旧数据时校正任务序号，防止重复使用已有 ID。
- 管理员 ID 按完整值匹配；管理员配置回退读取当前会话配置，群管理权限仅对群消息生效。
- 播报时间查询遵循查询权限配置；缺少会话标识时拒绝创建共享的空会话。

### 提醒与指令修复

- 发送提醒时校验任务快照和修订号，避免发送期间改期、修改或删除任务造成错误的提醒标记。
- 每条提醒成功后立即保存状态，避免后续发送被取消时丢失已完成的提醒记录。
- 带时刻的任务即使跨日，也只在到点提醒成功后自动清理；停用的任务继续保留。
- 每日播报仅清理本次实际发送且未被修改的任务，避免误删发送期间新增或修改的任务。
- `pre_remind_minutes=0` 正确关闭提前提醒，保留到点提醒。
- `catch_up_minutes=0` 仍在设定的播报分钟内发送，仅关闭之后的补发。
- 定时回调异常后保持等待；按会话处理发送异常，并为定时发送增加超时限制。
- 带时刻的任务在到点前不会因“剩余 0 天”提前触发每日播报的全体提醒。
- LLM 添加、修改工具直接处理结构化参数，保留名称与模板中的空格、引号和换行。
- 支持按带空格或书名号后缀的名称修改、删除和切换任务；无效日期不会再被吞入任务名称。

### 代码整理

- 将添加 / 修改校验与提醒投递逻辑提取到独立模块，便于在不导入 AstrBot 的情况下测试。
- 复用查询任务筛选逻辑，缓存渲染函数参数信息，减少重复序列化。
- 同步更新 WebUI 配置提示、使用文档和版本号。

### 兼容性与验证

- 平台仍为 aiocqhttp（OneBot v11），Python 要求仍为 3.10+；正常的旧版任务数据无需手动迁移。
- 模板格式宽度、精度和单条展开结果限制为 4096；无效格式按原文显示，超长原始模板会截断。
- 数据文件异常时，需修复数据或恢复备份后重载插件。
- 95 项单元及回归测试通过，Ruff 检查和格式检查通过。
- 已检查实际生成的普通卡片、长名称 / 多行内容、空列表及 30 条任务长图，并验证安装包结构与 Python 3.10 语法兼容性。
- 尚未在真实 AstrBot / QQ 实例中进行消息收发端到端验证。
